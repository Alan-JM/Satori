<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="@android:color/white">

    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <!-- Nombre del operador -->
        <TextView
            android:id="@+id/tvNombreDetalle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Nombre:"
            android:textSize="18sp"
            android:textStyle="bold"
            android:paddingBottom="8dp"/>

        <!-- Correo del operador -->
        <TextView
            android:id="@+id/tvCorreoDetalle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Correo:"
            android:textSize="16sp"
            android:paddingBottom="4dp"/>

        <!-- Teléfono del operador -->
        <TextView
            android:id="@+id/tvTelefonoDetalle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Teléfono:"
            android:textSize="16sp"
            android:paddingBottom="8dp"/>

        <!-- Kilómetros recorridos -->
        <TextView
            android:id="@+id/tvKmRecorridos"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Km recorridos: 0"
            android:textSize="16sp"
            android:paddingBottom="4dp"/>

        <!-- Combustible gastado -->
        <TextView
            android:id="@+id/tvCombustibleGastado"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Combustible gastado: 0"
            android:textSize="16sp"
            android:paddingBottom="4dp"/>

        <!-- Rendimiento -->
        <TextView
            android:id="@+id/tvRendimiento"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Rendimiento actual: 0 km/l"
            android:textSize="16sp"
            android:paddingBottom="4dp"/>

    </LinearLayout>
</ScrollView>
