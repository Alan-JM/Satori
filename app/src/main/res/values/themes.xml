<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema principal de la app -->
    <style name="Theme.Satori_by_Aristo" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Colores principales -->
        <item name="colorPrimary">#2A1B12</item>
        <item name="colorPrimaryVariant">@color/orange_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/grey</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- SOLUCIÓN: Control de barras del sistema y Action Bar -->
        <item name="android:statusBarColor">@color/naranja_boton</item> <!-- Color de la barra de la hora/batería -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item> <!-- Fuerza al sistema a respetar los límites -->
        <item name="windowActionBar">true</item>
        <item name="windowNoTitle">false</item>
    </style>

    <!-- Variante sin ActionBar (para usar tu MaterialToolbar) -->
    <style name="Theme.Satori_by_Aristo.NoActionBar" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/naranja_boton</item>
        <item name="colorPrimaryVariant">@color/naranja_boton</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/grey</item>
        <item name="colorOnSecondary">@color/black</item>
    </style>

</resources>
